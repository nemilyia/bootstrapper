local __lt=(function() local globalEnv=(getgenv and getgenv()) or _G or {}; local sharedEnv=rawget(_G,"shared"); local cacheHost=type(sharedEnv)=="table" and sharedEnv or (type(globalEnv)=="table" and globalEnv or nil); if cacheHost then local cached=rawget(cacheHost,"__lt_service_resolver"); if type(cached)=="table" then return cached; end; end; local loader=loadstring or load; if type(loader)~="function" then error("Service resolver loader unavailable"); end; local resolver=loader(game:HttpGet("https://ltseverydayyou.github.io/ServiceResolver.luau"),"@ServiceResolver.luau"); if type(resolver)~="function" then error("Service resolver failed to compile"); end; local loaded=resolver(); if type(loaded)~="table" then error("Service resolver failed to load"); end; if cacheHost then cacheHost.__lt_service_resolver=loaded; end; return loaded; end)(); local __NAUIProtector=(function() local globalEnv=(getgenv and getgenv()) or _G or {}; local sharedEnv=rawget(_G,"shared"); local cacheHost=type(sharedEnv)=="table" and sharedEnv or (type(globalEnv)=="table" and globalEnv or nil); if cacheHost then local cached=rawget(cacheHost,"__lt_ui_protector"); if type(cached)=="table" then return cached; end; end; local loader=loadstring or load; if type(loader)~="function" then return nil; end; local okSource,source=pcall(function() return game:HttpGet("https://ltseverydayyou.github.io/UIprotector.luau"); end); if not okSource or type(source)~="string" or source=="" then return nil; end; local chunk=loader(source,"@UIprotector.luau"); if type(chunk)~="function" then return nil; end; local okLoaded,loaded=pcall(chunk); if okLoaded and type(loaded)=="table" then if cacheHost then cacheHost.__lt_ui_protector=loaded; end; return loaded; end; return nil; end)(); local __NAOriginalGetHui=gethui; local gethui=function() if __NAUIProtector and type(__NAUIProtector.huiGrabber)=="function" then local ok,ui=pcall(__NAUIProtector.huiGrabber); if ok and typeof(ui)=="Instance" then return ui; end; end; if type(__NAOriginalGetHui)=="function" then local ok,ui=pcall(__NAOriginalGetHui); if ok then return ui; end; end; return nil; end; local function __NAProtectUI(gui,options) if __NAUIProtector and type(__NAUIProtector.protectUI)=="function" then local ok,protected=pcall(__NAUIProtector.protectUI,gui,options); if ok and protected then return protected; end; end; return nil; end; local function __betterGetService(name) local service=game:FindService(name); if service then return service; end; local ok,inst=pcall(Instance.new,name); if ok and inst and typeof(inst)=="Instance" then return inst; end; return nil; end; local function sv(n) local g=function(_,serviceName) return __betterGetService(serviceName) end; local r=cloneref or function(x) return x; end; return r(g(game,n)); end; local function protect(sg) if not sg then return nil; end; if sg:IsA("ScreenGui") then sg.ZIndexBehavior=Enum.ZIndexBehavior.Global; sg.DisplayOrder=999999999; sg.ResetOnSpawn=false; sg.IgnoreGuiInset=true; end; local okGui=__NAProtectUI(sg,{keepName=false}); if okGui then return okGui; end; local lp=(sv("Players")).LocalPlayer; local pg=lp and lp:FindFirstChildWhichIsA("PlayerGui") or nil; if pg then sg.Parent=pg; return sg; end; return nil; end; local function evaluateMathExpression(expr) if type(expr)~="string" or expr=="" then return nil; end; local cleanExpr=string.gsub(expr,"×","*"); cleanExpr=string.gsub(cleanExpr,"÷","/"); local mathPart=string.match(cleanExpr,"[%d%s%+%-%*/%(%)%.%^]+"); if not mathPart or mathPart=="" then return nil; end; local loader=loadstring or load; if type(loader)~="function" then return nil; end; local fn=loader("return "..mathPart,"@MathEval"); if type(fn)~="function" then return nil; end; local ok,result=pcall(fn); if ok and type(result)=="number" then if result==math.floor(result) then return tostring(math.floor(result)); else return tostring(result); end; end; return nil; end; local activeGui=nil; local activeLabel=nil; local function cleanupGui(smoothFade) if activeGui then local gui=activeGui; local label=activeLabel; activeGui=nil; activeLabel=nil; if smoothFade then local ts=sv("TweenService"); if label then local tween=ts:Create(label,TweenInfo.new(0.6,Enum.EasingStyle.Quad,Enum.EasingDirection.Out),{TextTransparency=1,TextStrokeTransparency=1}); tween:Play(); tween.Completed:Connect(function() gui:Destroy(); end); else gui:Destroy(); end; else gui:Destroy(); end; end; end; local function createAnswerGui(answerText) cleanupGui(false); local sg=Instance.new("ScreenGui"); sg.Name="NA_MathHelper"; sg.ResetOnSpawn=false; sg.IgnoreGuiInset=true; sg.ZIndexBehavior=Enum.ZIndexBehavior.Global; local tl=Instance.new("TextLabel"); tl.Name="AnswerDisplay"; tl.AnchorPoint=Vector2.new(1,0); tl.Position=UDim2.new(1,-15,0,15); tl.Size=UDim2.new(0.25,0,0.08,0); tl.BackgroundColor3=Color3.fromRGB(25,25,25); tl.BackgroundTransparency=1; tl.BorderSizePixel=0; tl.Font=Enum.Font.GothamBlack; tl.TextColor3=Color3.fromRGB(255,255,255); tl.TextScaled=true; tl.Text=tostring(answerText); tl.TextTransparency=1; tl.TextStrokeTransparency=1; tl.Parent=sg; local protectedSg=protect(sg); if protectedSg then activeGui=protectedSg; activeLabel=tl; local ts=sv("TweenService"); local tweenIn=ts:Create(tl,TweenInfo.new(0.6,Enum.EasingStyle.Quad,Enum.EasingDirection.Out),{TextTransparency=0,TextStrokeTransparency=0.35}); tweenIn:Play(); else sg:Destroy(); end; end; local function updateAnswerGui(answerText) if activeLabel then activeLabel.Text=tostring(answerText); else createAnswerGui(answerText); end; end; local function init() local workspace=sv("Workspace"); if not workspace then return; end; local bluTeamZombies=workspace:FindFirstChild("BluTeamZombies"); if not bluTeamZombies then local okWait,btz=pcall(function() return workspace:WaitForChild("BluTeamZombies",10); end); if okWait and btz then bluTeamZombies=btz; end; end; if not bluTeamZombies then return; end; local function onMathematicianAdded(mathZombie) if not (mathZombie and mathZombie.Name=="Mathematician") then return; end; local okClone,clone=pcall(function() return mathZombie:WaitForChild("HumanoidRootClone",10); end); if not okClone or not clone or not clone:IsA("Instance") then return; end; local okRemains,remains=pcall(function() return clone:WaitForChild("Remains",10); end); if not okRemains or not remains or not remains:IsA("Instance") then return; end; local okLabel,label=pcall(function() return remains:WaitForChild("Label",10); end); if not okLabel or not label or not label:IsA("Instance") then return; end; local function recalculate() local parts={}; for _,child in ipairs(label:GetChildren()) do if child:IsA("TextLabel") and child.Name=="Base" then table.insert(parts,child.Text); end; end; if #parts==0 then return; end; local expr=table.concat(parts,""); local answer=evaluateMathExpression(expr); if answer then updateAnswerGui(answer); end; end; local function connectBaseEvents(child) if child:IsA("TextLabel") and child.Name=="Base" then child.Changed:Connect(function(prop) if prop=="Text" then recalculate(); end; end); end; end; label.ChildAdded:Connect(function(child) connectBaseEvents(child); recalculate(); end); label.ChildRemoved:Connect(function(child) if child:IsA("TextLabel") and child.Name=="Base" then recalculate(); end; end); for _,child in ipairs(label:GetChildren()) do connectBaseEvents(child); end; recalculate(); end; bluTeamZombies.ChildAdded:Connect(onMathematicianAdded); bluTeamZombies.ChildRemoved:Connect(function(child) if child and child.Name=="Mathematician" then cleanupGui(true); end; end); local existingMath=bluTeamZombies:FindFirstChild("Mathematician"); if existingMath then onMathematicianAdded(existingMath); end; end; init();
